#!/bin/bash
# Git pre-push hook
# Optional: Run additional checks before pushing
#
# This file is managed in templates/common/.githooks/
# Install using: ./.claude/scripts/install-hooks.sh

set -e

# Color codes
YELLOW='\033[1;33m'
GREEN='\033[0;32m'
NC='\033[0m'

echo -e "${YELLOW}Running pre-push checks...${NC}"

# Example: Run tests before push (uncomment if needed)
# if command -v pytest &> /dev/null; then
#     echo "Running tests..."
#     pytest
# fi

# Example: Check if main branch (prevent accidental push to main)
# BRANCH=$(git rev-parse --abbrev-ref HEAD)
# if [ "$BRANCH" = "main" ] || [ "$BRANCH" = "master" ]; then
#     echo -e "${YELLOW}Warning: Pushing to $BRANCH${NC}"
#     read -p "Are you sure? (y/n) " -n 1 -r
#     echo
#     if [[ ! $REPLY =~ ^[Yy]$ ]]; then
#         exit 1
#     fi
# fi

echo -e "${GREEN}Pre-push checks passed${NC}"
exit 0
